import styled from './FooterLayout.module.scss';

export default function FooterLayout() {
  return (
    <footer className={`${styled.footer}`} data-theme="light">
      <div className={`${styled['container-footer']}`}>
        xd
      </div>
    </footer>
  )
}